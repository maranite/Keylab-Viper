/** Produces a "Not defined" error in Bitwig 3.02 */
declare enum HardwareControlType {
    KNOB, // Absolute control
    SLIDER, // Absolute control
    ENCODER, // Relative control (never shows takeover-indication)
}

